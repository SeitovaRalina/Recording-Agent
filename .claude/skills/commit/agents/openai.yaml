interface:
  display_name: "Conventional Commit"
  short_description: "Create detailed Conventional Commits without pushing"
  default_prompt: "Use $commit to create a detailed Conventional Commit from the staged diff, validate scope and breaking-change markers, commit locally, and never push."
